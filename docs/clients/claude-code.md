# Claude Code

Prefer the plugin-hosted Streamable HTTP endpoint:

```bash
claude mcp add --transport http zotero-local-mcp-bridge http://127.0.0.1:23119/zotero-local-mcp-bridge/mcp
```

The stdio adapter is also available for environments where HTTP MCP is unavailable.
