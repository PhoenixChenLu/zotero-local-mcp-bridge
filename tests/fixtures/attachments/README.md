# Attachment Fixtures

这些文件用于 `ZoteroLocalMcpBridgeTest` 手工验收，不能用于真实主库。

当前仓库内提供文本可审查 fixtures：

- `sample-paper.pdf`
- `sample-data.csv`
- `sample-page.html`
- `sample-image.svg`

DOC、DOCX、XLS、XLSX 需要在本机用 Office、LibreOffice 或其它可靠工具生成真实二进制文件后再加入验收。不要用伪造文本文件冒充这些格式。
